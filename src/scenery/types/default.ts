import * as BABYLON from "babylonjs";
import Structure from "app/shared/structure";

export const name = "default"

/**
 * Default class linked to mesh
 */
export default class Default extends Structure {

}